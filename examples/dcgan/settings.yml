train:
  data_provider:
    D:
      real_train:
        cate: ItemLoader
        num_samples: 1
        data_key: "data"
        target_key: "target"
      fake:
        cate: FakeSampler
        num_samples: 1
        data_key: "data"
        target_key: "target"
    G:
      noise:
        cate: GaussianNoiseSampler
        num_samples: 1
        data_key: "latent"
        target_key: "valid"
eval:
  data_provider:
    G:
      noise:
        cate: GaussianNoiseSampler
        num_samples: 1
        data_key: "latent"
        target_key: "valid"
  visualizers:
    - generation
    - meters
