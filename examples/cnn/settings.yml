train:
  data_provider:
    mymodel:
      mnist_train:
        cate: ItemLoader
        num_samples: 1
        data_key: "data"
        target_key: "target"
eval:
  data_provider:
    mymodel:
      mnist_eval:
        cate: ItemLoader
        num_samples: 1
        data_key: "data"
        target_key: "target"
