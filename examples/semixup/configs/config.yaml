defaults:
  - args: args1
  - sampling: sampling1

strategy:
  # Support 'train' or 'eval' stages only
  stage_names:
    - train
    - eval

  accumulate_grad:
    efficientnet: False

  accumulate_grad_in_iter:
    efficientnet: False

  train_starts_at_epoch:
    efficientnet: 0
